# Runbook: LLM 503 / generation degraded

## Symptoms
- HTTP 503 on `POST /v1/generate`
- Queue timeout errors in logs
- `generation` component degraded on status page

## Triage
1. Check provider status (Google AI / Anthropic)
2. `kill_switch` — `today_spend_usd` near `daily_spend_cap_usd`?
3. Redis LLM slots: `PLG_LLM_QUEUE_SLOTS` saturated?
4. Rate limit false positives — user banned?

## Mitigations
1. **Raise cap temporarily** (if spend legit): update `daily_spend_cap_usd`
2. **Switch provider**: set `PLG_LLM_PROVIDER` fallback env
3. **Reduce queue**: lower concurrent slots to avoid provider ban
4. **Kill switch**: disable generation with user-facing message if costs runaway

## Comms
- Status: "AI generation slow — retries automatic"
- Do not promise exact ETA from OpenAI/Google
